using System.Linq;
using System.Runtime.InteropServices;
using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class OrganizationLogoGetter : MonoBehaviour
{

    public Button organizationButton;
    public Canvas firstSceneCanvas;
	// Use this for initialization
	void Start ()
	{
//        string texture = "Assets/Resources/monkey_64px.png";
//        Texture2D inputTexture = (Texture2D)Resources.LoadAssetAtPath(texture, typeof(Texture2D));
//
//        renderer.material.mainTexture = inputTexture;
//        var rc = new Rect(0, 0, inputTexture.width, inputTexture.height);
//        var vc = new Vector2(0.5f, 0.5f);
//        Sprite sp = Sprite.Create(inputTexture, rc, vc);
//        var btn = gameObject.GetComponent<Button>();
//        btn.image.sprite = sp;
//	    var a = firstSceneCanvas.GetComponent<OrganizationDeserializer>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
