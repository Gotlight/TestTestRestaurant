using System.Collections;
using UnityEngine;
using System.Collections.Generic;

public class ModelRestaurant : MonoBehaviour
{
    // every RestaurantModel object will have different menus - e.g. First courses, Wines. menuList will contain them
    private List<ModelMenu> menuList;
    
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void GetMenusFromServer()
    {
        
    }
}
